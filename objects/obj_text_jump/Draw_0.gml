// Draw Event
draw_set_color(cor); // Define a cor
draw_set_font(fonte); // Define a fonte (se não for -1)
draw_set_halign(alinhamento); // Define o alinhamento
draw_text(x, y, texto); // Desenha o texto na posição (x, y) do objeto